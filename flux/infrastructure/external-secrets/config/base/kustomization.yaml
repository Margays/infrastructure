apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cluster_secret_store.yaml

patches:
  - target:
      kind: ClusterSecretStore
      name: vault
    patch: |-
      - op: replace
        path: /spec/provider/vault/mountPath
        value: "kubernetes/monitoring"
