apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kagent-crds
spec:
  chart:
    spec:
      chartRef:
        kind: OCIRepository
        name: kagent-crds
  values: {}
  interval: 1h
