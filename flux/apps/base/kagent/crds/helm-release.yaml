apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kagent-crds
spec:
  chart:
    spec:
      chart: cert-manager
      sourceRef:
        kind: OCIRepository
        name: kagent-crds
      version: 0.9.12
  values: {}
  interval: 1h
